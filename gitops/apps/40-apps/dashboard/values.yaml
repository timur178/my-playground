image:
  repository: ${ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/${TEAM}/${ENVIRONMENT_STAGE}/kafka-dashboard
  tag: "1.0.0"
replicaCount: 1
service:
  type: ClusterIP
  port: 3000
ingress:
  enabled: true
  className: nginx
  host: kafka-dashboard-${TEAM}-dev.${TEAM_DOMAIN}

syncWave: "4"

